import {defineBookPage} from 'element-book';
import {type BookGlobals} from './book-globals.js';

export const elementsPage = defineBookPage<BookGlobals>({
    parent: undefined,
    title: 'Elements',
});
