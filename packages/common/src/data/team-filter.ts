export enum TeamFilter {
    Active = 'active',
    /** An internal admin team. */
    Admin = 'admin',
    /** Teams approved by admin. */
    Approved = 'approved',
    Deactivated = 'deactivated',
    NotApproved = 'not-approved',
    Test = 'test',
}
